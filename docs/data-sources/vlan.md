---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pfsense_vlan Data Source - terraform-provider-pfsense"
subcategory: ""
description: |-
  Retrieves a single VLAN https://docs.netgate.com/pfsense/en/latest/interfaces/vlan.html by its interface name (e.g. vmx0.100). VLANs allow segmenting a physical network into virtual networks using 802.1Q tagging.
---

# pfsense_vlan (Data Source)

Retrieves a single [VLAN](https://docs.netgate.com/pfsense/en/latest/interfaces/vlan.html) by its interface name (e.g. `vmx0.100`). VLANs allow segmenting a physical network into virtual networks using 802.1Q tagging.

## Example Usage

```terraform
data "pfsense_vlan" "example" {
  vlan_interface = "igb0.100"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `vlan_interface` (String) Computed VLAN interface name (e.g. 'vmx0.100'). This is the OS-level interface created for the VLAN.

### Read-Only

- `description` (String) For administrative reference (not parsed).
- `parent_interface` (String) Parent physical interface for the VLAN (e.g. 'vmx0', 'igb1').
- `pcp` (Number) VLAN Priority Code Point (802.1p), between 0 and 7.
- `tag` (Number) VLAN tag (ID), must be between 1 and 4094.
